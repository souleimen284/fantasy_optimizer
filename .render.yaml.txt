services:
  - type: web
    name: fantasy-api
    runtime: python
    buildCommand: ./render-build.sh && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
